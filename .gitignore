# Upstream stays as its own repo (not committed into distro)
upstream/

# Keep reverse-engineering artifacts out of the distro repo
research/

# Local noise
**/.DS_Store
lm-bridge.code-workspace

# Deploy outputs
deploy/.env
dist/

# Plugin local backup
lm-bridge-plugin/Submodules/Lidarr.bak/

# Plugin build output
lm-bridge-plugin/plugin/bin/
lm-bridge-plugin/plugin/obj/
lm-bridge-plugin/dist/
lm-bridge-plugin/plugin/PluginInfo.cs

# never commit upstream edits; Dockerfile is tracked upstream
!upstream/lidarr-metadata/Dockerfile

# allow upstream submodule
!upstream/
!upstream/lidarr-metadata/
